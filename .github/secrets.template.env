# GitHub Actions Secrets Template
# Copy this file and fill in your values
# DO NOT commit actual secrets to the repository!

# ============================================
# Docker Registry Credentials
# ============================================

# Option 1: Docker Hub
DOCKER_USERNAME=your-dockerhub-username
DOCKER_PASSWORD=your-dockerhub-access-token
# Get access token from: https://hub.docker.com/settings/security

# Option 2: GitHub Container Registry (GHCR)
GHCR_USERNAME=${{ github.actor }}
DOCKER_PASSWORD=${{ secrets.GITHUB_TOKEN }}
# Note: GITHUB_TOKEN is automatically provided by GitHub Actions

# Option 3: Google Container Registry (GCR)
GCR_SERVICE_ACCOUNT_KEY=base64-encoded-service-account-json
# Generate service account key from Google Cloud Console
# Then: cat service-account.json | base64 -w 0

# Option 4: Amazon ECR
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1

# ============================================
# Kubernetes Configuration
# ============================================

KUBE_CONFIG=base64-encoded-kubeconfig
# Generate: cat ~/.kube/config | base64 -w 0 (Linux)
#          cat ~/.kube/config | base64 (macOS)

# ============================================
# Notification Services (Optional)
# ============================================

# Slack
SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
# Get webhook URL from: https://api.slack.com/messaging/webhooks

# Discord
DISCORD_WEBHOOK=https://discord.com/api/webhooks/YOUR/WEBHOOK
# Get webhook from Discord Server Settings → Integrations → Webhooks

# Email (Gmail example)
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
# Gmail: Use App Password (not regular password)
# Generate from: https://myaccount.google.com/apppasswords

# ============================================
# Cloud Provider Specific (Optional)
# ============================================

# Google Cloud Platform
GOOGLE_CREDENTIALS=base64-encoded-service-account-json
GCP_PROJECT_ID=your-project-id
GKE_CLUSTER_NAME=your-cluster-name
GKE_ZONE=us-central1-a

# Amazon Web Services
AWS_ACCOUNT_ID=123456789012
EKS_CLUSTER_NAME=your-cluster-name
EKS_REGION=us-east-1

# Microsoft Azure
AZURE_CREDENTIALS=azure-service-principal-json
AKS_RESOURCE_GROUP=your-resource-group
AKS_CLUSTER_NAME=your-cluster-name

# ============================================
# How to Add Secrets to GitHub
# ============================================

# 1. Go to your GitHub repository
# 2. Click Settings → Secrets and variables → Actions
# 3. Click "New repository secret"
# 4. Add each secret with the name (e.g., DOCKER_USERNAME) and value
# 5. Click "Add secret"

# ============================================
# Testing Secrets Locally (Optional)
# ============================================

# For testing GitHub Actions locally with 'act':
# 1. Create a file: .secrets
# 2. Add secrets in format: SECRET_NAME=value
# 3. Run: act -s $(cat .secrets)
# 4. DO NOT commit .secrets file!

# ============================================
# Security Best Practices
# ============================================

# ✅ DO:
# - Use access tokens instead of passwords
# - Rotate secrets regularly
# - Use separate credentials for CI/CD
# - Limit permissions to minimum required
# - Use environment-specific secrets

# ❌ DON'T:
# - Commit secrets to repository
# - Share secrets in plain text
# - Use production credentials in development
# - Hard-code secrets in workflow files
# - Give more permissions than needed
